package ex3_inheritance;

public class Child extends Parent {	//자바에선 단일상속체계 : 객체는 반드시 하나의 부모클래스만 존재할 수 있다. 단, 부모의 부모..등은 존재 가능
	//상속 : 부모의 재산을 자식이 물려받는 것
	//extends키워드를 통해 Child를 Parent의 자식 클래스로 지정할 수 있다.
	String car = "티코";
	
}
